import { Spec } from 'axe-core';
import { Browser, Page } from 'puppeteer';
import { ResultByUrl } from '../models/a11y-sitechecker-result';
import { Config, SitecheckerViewport } from '../models/config';
export declare function analyzeSite(axeSpecs: Spec, firstpage: Page, config: Config, resultsByUrl: (ResultByUrl | null)[], browser: Browser, alreadyVisited: Map<string, SitecheckerViewport>, alreadyParsed: string[], notCheckedLinks: string[], link?: string): Promise<ResultByUrl[]>;
