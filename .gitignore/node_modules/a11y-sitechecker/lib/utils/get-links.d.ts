import { Config, SitecheckerViewport } from '../models/config';
export interface RootDomain {
    value: string;
}
export declare function getLinks(html: string, url: string, config: Config, alreadyParsed: string[], rootDomain: RootDomain, elementsToClick: Map<string, string[]>, notCheckedLinks: string[], alreadyVisited: Map<string, SitecheckerViewport>): string[];
