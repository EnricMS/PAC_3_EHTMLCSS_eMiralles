import { Spec } from 'axe-core';
import { Config } from '../models/config';
import { OptionValues } from 'commander';
import { Page } from 'puppeteer';
import { AxePuppeteer } from '@axe-core/puppeteer';
export declare function setupConfig(options: OptionValues): Config;
export declare function prepareWorkspace(config: Config): void;
export declare function setupAxeConfig(config: Config): Spec;
export declare function setupAxe(page: Page, axeSpecs: Spec, config: Config): Promise<AxePuppeteer>;
